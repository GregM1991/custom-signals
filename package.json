{
  "name": "custom-signals",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/server/server.ts",
    "build": "bun build src/client/app.ts --outdir=dist"
  },
  "module": "app.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}